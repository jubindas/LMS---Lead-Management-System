export type Enquiry = {
  sl: number;
  Name: string;
  phone: string;
  user: string;
  status: string;
  stage: string; // <-- Make stage editable
  budget: string;
  businessType: string;
  source: string;
};
